# MCP Examples

These are starter JSON snippets. Do not commit real API keys or absolute paths to a shared repo. Copy the snippet you need into your Claude Desktop / Windsurf / Codex `mcp_config.json` and fill in `<PLACEHOLDERS>` before enabling the server.

Enable only the smallest set that matches the active profile. Disable heavy servers when not in use.
